Goal: make the / fuzzy-find actually usable by handing the terminal to fzf cleanly instead of running it with inherited stdio while Bubble Tea owns the alt-screen.

WHY: Task 16 implemented / per the plan (inherited stdio). Code review assessment: Bubble Tea input reader + render loop contend with fzf for the tty, so the interactive picker is likely non-functional (garbled / dead input) even though the jump LOGIC (jumpMsg/IndexOfID) is correct and tested.

FIX: convert findCmd to use tea.ExecProcess(cmd, func(err error) tea.Msg{...}). Bubble Tea releases the terminal (exits raw mode + alt-screen), hands tty to fzf, restores itself on exit. Need to capture fzf stdout — tea.ExecProcess gives the process the real stdio, so capture selection via a temp file (fzf --print-query or redirect) or an os.Pipe, then parse with shell.ParseFzfSelection and emit jumpMsg.

Files: internal/ui/model.go (findCmd), maybe internal/shell/fzf.go (a Pick variant that returns the *exec.Cmd + a result accessor rather than running it inline).
Verify: go test ./... green; then MANUAL: run the TUI in a real yaks repo, press /, confirm fzf renders cleanly and selecting a yak moves the cursor.
Plan: follow-up to Task 16 (docs/superpowers/plans/2026-06-04-yaks-tui.md).
