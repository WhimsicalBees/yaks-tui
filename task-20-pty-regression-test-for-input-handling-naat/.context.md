Goal: an automated test that would have caught the input-lag bug (commit fff6ada) — i.e. catches any regression where the render/event loop issues a terminal query that reads stdin and races Bubble Tea.

Approach: integration test that runs the real binary under a PTY which ANSWERS the startup OSC 11 background-color query, then sends navigation keystrokes WITHOUT answering further queries, and asserts NO additional OSC 11 query (\x1b]11;?) appears in output produced after the keystrokes (the regression signal), plus liveness (program redraws after keys). Skip gracefully if yx missing / PTY unavailable / testing.Short().

Why this guard: the whole bug class is invisible to non-TTY go test. A query-answering PTY harness is the only thing that exercises the real terminal path.

Files: a new root-package integration test (e.g. main_pty_test.go), test dep github.com/creack/pty.
Verify: go test ./... stays green; the new test PASSES on current (fixed) code and would FAIL against the old WithAutoStyle-in-render-loop behavior.
