NEEDS HUMAN VERIFICATION at a real terminal (cannot be reliably automated — Bubble Tea blocks on terminal device-attribute queries that a hand-rolled PTY harness does not answer, so automated full-TUI drive-throughs are not faithful).

The fzf MECHANISM is already verified twice via PTY (candidate list in / selection out / interactive on tty / exit-130 cancel). What remains is the live hand-off inside the running Bubble Tea program.

CHECKLIST (run `./bin/yaks-tui` in a real terminal inside a yaks repo):
1. Press / → Bubble Tea cleanly releases the alt-screen and fzf takes over (no garbled/overlapping render).
2. Type to filter, Enter to select → cursor jumps to that yak, alt-screen TUI restores intact.
3. Esc/Ctrl-C in fzf → returns to TUI, no cursor move, no leftover artifacts.
4. (fzf not installed) → status bar shows the friendly errMsg, UI keeps working.
