Mode-owns-keyboard blocks at top of handleKey: enter commits (empty=cancel), esc cancels; y confirms remove (recursive=kids>0), else cancel. addCmd/renameCmd/removeCmd reload preserving id. existingIDs walker.

Plan: docs/superpowers/plans/2026-06-11-add-rename-remove.md (Task 7)
