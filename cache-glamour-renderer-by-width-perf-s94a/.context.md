FUTURE OPTIMIZATION (not v1-blocking; from Task 12 review).

refreshDetail (internal/ui/model.go) calls renderMarkdown, which builds a fresh glamour.NewTermRenderer on EVERY cursor move. Renderer construction parses a full style theme — measurable allocation per keystroke.

Fix: cache the *TermRenderer on the Model, keyed by width; rebuild only when the detail pane width changes (resize). Acceptable as-is for v1 triage navigation.
