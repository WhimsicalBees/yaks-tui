FUTURE OPTIMIZATION (not blocking; from Task 12 review). PARTIALLY ADDRESSED by the input-lag fix (commit fff6ada): glamour style is now resolved ONCE at startup and rendering uses WithStandardStyle, so NewTermRenderer no longer does any terminal I/O. What remains is purely CPU: refreshDetail still rebuilds the *TermRenderer (parses the style config) on every cursor move. Cache the renderer on the Model, keyed by (style, width), rebuild only on resize. Lower priority now that the blocking query is gone.
