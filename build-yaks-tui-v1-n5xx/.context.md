Top-level goal: a keyboard-driven, two-pane TUI over the yx CLI for browsing the yak tree and triaging state.

yx is the only thing that touches .yaks/ (all reads via `yx ... --format json`, all writes via yx subcommands). Markdown rendered in-process with glamour; fuzzy find via fzf.

Full spec: docs/superpowers/specs/2026-06-04-yaks-tui-design.md
Full plan: docs/superpowers/plans/2026-06-04-yaks-tui.md

Children (phases) are prerequisites; work them in order 0..5.
