Pure id helpers in new internal/yaks/ids.go: slugify (lowercase, non-alnum→-, trim, fallback 'yak'), randomSuffix (4-char), uniqueID (slug+suffix, regenerate on collision, injectable gen for tests). TDD.

Plan: docs/superpowers/plans/2026-06-11-add-rename-remove.md (Task 1)
