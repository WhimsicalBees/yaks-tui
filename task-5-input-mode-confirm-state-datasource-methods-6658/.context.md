model.go: inputMode enum + fields (inputParID/inputTgtID/input), confirm fields (removeID/Name/Kids); init textinput in New; widen dataSource interface; extend stubClient. Build+existing tests gate.

Plan: docs/superpowers/plans/2026-06-11-add-rename-remove.md (Task 5)
