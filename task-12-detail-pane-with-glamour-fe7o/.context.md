Goal: detailMarkdown(yak) + glamour render with raw-text fallback; render from list JSON.
Files: internal/ui/detailpane.go, model.go (refreshDetail)
Verify: go test ./internal/ui/ -run TestDetailMarkdown -v
Plan: docs/superpowers/plans/2026-06-04-yaks-tui.md (Task 12)

REVIEW FOLLOW-UP (from Task 10 code review): when replacing the refreshDetail stub, align its cursor guard to both-ends like selectedID (currently only checks cursor < len(rows), missing cursor >= 0). Safe today via ClampCursor but inconsistent.
